import { ethers } from 'ethers';
export declare const reencryptRequest: (kmsSignatures: string[], chainId: number, kmsContractAddress: string, aclContractAddress: string, gatewayUrl: string, provider: ethers.JsonRpcProvider | ethers.BrowserProvider) => (handle: bigint, privateKey: string, publicKey: string, signature: string, contractAddress: string, userAddress: string) => Promise<bigint>;
